# PrintProof dependencies
# Install with: pip3 install --user -r requirements.txt

# Required for Excel batch parsing
openpyxl

# Optional OCR-only extras
pytesseract

# OpenCV is intentionally not listed here.
# On Jetson, use system package for GStreamer camera support:
#   sudo apt install python3-opencv
